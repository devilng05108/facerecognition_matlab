Soft computing group assignment

1. Run training with file FaceRecognition_final.m
- This will train and validate the model. At the end of training, a windows will open with webcam (if there it has webcam access). The webcam will take a screen shot if it detects a face. The name of the person will be shown above the face if the model manage to classify the face.

2. webcamTest.m can be run after the model has been trained. It will do the second part as mentioned in (1).